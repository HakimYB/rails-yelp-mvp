<div class= "title">
  <h1>Write your review</h1>
</div>

<div class="margin">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit %>
  <% end %>
</div>

<%= link_to "⬅ Return to home page", restaurants_path(), class: "btn btn-primary"%>
